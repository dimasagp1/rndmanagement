Three-file MVC slice: `MaterialController` implements a standard RESTful resource (`index`, `create`, `store`, `edit`, `update`, `destroy`) backed by route names under the `materials.*` namespace; it renders Blade views in the `materials.*` view folder and flashes Indonesian success messages via `with('success', ...)`. The `Material` Eloquent model declares `$fillable` fields matching the controller's validation rules and integrates Spatie Activitylog through the `LogsActivity` trait, logging only dirty changes to `name`, `type`, and `unit` via `getActivitylogOptions()`. It also exposes a `hasMany(FormulaMaterial::class)` relation, indicating the model participates in the formula composition domain. The schema is defined in the `2026_07_01_092816_create_materials_table.php` migration with columns `name`, `type`, `unit` (default `'kg'`), and `description`, plus Laravel's default `timestamps()` — no soft deletes or foreign keys are declared at this level.